version: 2

macros:
  - name: grant
    description: "Applies role grants to the attached model in the data warehouse."
    arguments:
      - name: role
        type: string
        description: "The name of the role you would like to grant."